#include <stdio.h>
#include <string.h>

int main()
{
    int a = (1 + 1 == 2); // a = 1;
    int b = (2 / 1 == 2) + (3 + 2 == 5) + (6 - 1 == 7) + (1 + 1 == 0); // b = 2;
    // 也就是说，要判断五个条件中是否至少有两个满足，不需要列举出所有可能的情况；
    // 只需要把每个条件的逻辑表达式加起来，判断最后加的结果是否大于 2 即可！

    // 仅为演示代码
}
