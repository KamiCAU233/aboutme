#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(void) {
    int n;
    cin >> n;
    vector<string> s;
    for (int i = 0; i < n; i++) {
        cin >> s[i];
    }
    // 这已经不是人可以写出来的代码了
}
