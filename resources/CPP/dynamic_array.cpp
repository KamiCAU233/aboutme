// This is the full version of a Dynamic Array taught in the course.
#include <iostream>
using namespace std;

template <class T>
class DymamicArray {
    private:
        unsigned int size;
        unsigned int capacity;
        T* array;
    public:
        // Constructor
        DynamicArray(unsigned int length = 0, T* arr);

        // Copy Constructor
        DynamicArray(DynamicArray darr);

        // Value Assignment
        DynamicArray& operator=(const DynamicArray )

        // Destructor


        // Index Method


        // Methods

}

